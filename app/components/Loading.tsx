import { ArrowPathIcon } from "@heroicons/react/24/outline";

const Loading = () => {
    return (
        <div className="flex items-center gap-2 m-auto w-full"><ArrowPathIcon className="size-4 animate-spin" />Loading...</div>
    )
}

export default Loading